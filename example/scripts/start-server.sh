#!/usr/bin/env bash
set -euo pipefail

# 示例脚本：真实项目应替换为当前 NestJS 后端实际启动命令。
# 目标：构建/启动 server，并提供健康检查入口。

echo "Starting NestJS server example..."
echo "1. Check env: server/.env.local"
echo "2. Run: pnpm --dir server start:dev"
echo "3. Health check: curl -f http://localhost:3000/api/health"

