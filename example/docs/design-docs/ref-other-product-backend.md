# 参考项目：other-product-backend

用途：参考相邻产品后端的 NestJS 模块划分、DTO、错误码和权限点。

位置：`reference-projects/other-product-backend/`

使用规则：

- 只读参考，当前项目以 `server/` 源码为准。
- 可参考错误码命名和模块边界，但不能直接假设数据库表、env、脚本一致。
- 如果参考项目规则和当前 `AGENTS.md` 冲突，以当前项目为准。

